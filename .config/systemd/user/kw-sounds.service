[Unit]
Description=UI event sounds: sample-cache preload + Hyprland/USB/network watchers (kw-sound)
After=network.target

[Service]
ExecStart=%h/.local/bin/kw-sound-monitor
Restart=on-failure

[Install]
WantedBy=default.target
