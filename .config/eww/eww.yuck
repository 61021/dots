;; -----------------------------------------------------------------------------
;; Eww config entry point. Each feature lives in its own file.
;; -----------------------------------------------------------------------------

(include "sidebar.yuck")
(include "bar.yuck")
(include "calendar.yuck")
